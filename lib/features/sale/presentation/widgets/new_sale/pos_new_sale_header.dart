import 'package:flutter/material.dart';

class PosNewSaleHeader extends StatelessWidget {
  const PosNewSaleHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

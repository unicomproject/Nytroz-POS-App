import 'tenant_admin_app_header.dart';

/// Shared black Tenant Admin header (approved catalogue name).
class TenantAdminHeader extends TenantAdminAppHeader {
  const TenantAdminHeader({
    super.key,
    super.onMenuPressed,
  });
}
